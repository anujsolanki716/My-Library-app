import { Role, User, Book, BorrowedBookRecord } from './types';

export const APP_NAME = "My Library";
export const API_BASE_URL = 'http://localhost:5001/api'; // Assuming backend runs on port 5001

// INITIAL_USERS, INITIAL_BOOKS, INITIAL_BORROWED_RECORDS, LOCAL_STORAGE_KEYS are removed
// as data will now be managed by the backend server and database.
